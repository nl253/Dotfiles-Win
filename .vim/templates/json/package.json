{
  "name": "{{ title }}",
  "description": "{{ description }}",
  "version": "{{ version }}",
  "main": "src/index.js",
  "license": "{{ license }}",
  "scripts": {
    "test": "npx jest",
    "doc": "npx jsdoc -c conf.json $(npm run files)",
    "prettify": "npx prettier --write $(npm run files)",
    "lint": "npx eslint $(npm run files)"
  },
  "repository": {
    "type": "git",
    "url": "{{ repo }}"
  },
  "keywords": ["{{ keywords }}"],
  "author": "{{ author }}",
  "license": "{{ license }}",
  "bugs": {
    "url": "{{ issues }}"
  },
  "homepage": "{{ website }}",
  "dependencies": {},
  "devDependencies": {
    "eslint": "*",
    "eslint-config-airbnb": "*",
    "eslint-config-prettier": "*",
    "eslint-plugin-compat": "*",
    "eslint-plugin-filenames": "*",
    "eslint-plugin-import": "*",
    "eslint-plugin-jsx-a11y": "*",
    "eslint-plugin-node": "*",
    "eslint-plugin-optimize-regex": "*",
    "eslint-plugin-prettier": "*",
    "eslint-plugin-promise": "*",
    "eslint-plugin-react": "*",
    "eslint-plugin-security": "*",
    "jest": "*",
    "jsdoc": "*",
    "markdownlint": "*",
    "prettier": "*",
    "semver": "*",
  }
}
